//$(document).ready(function() { //ready(): 문서가 모두 로딩되면 호출해줌
//	$('.poem-stanza').addClass('highlight'); //poem-stanza를 클래스로 가지고 있는 것들의 집합
//});

$(function() { //$(document).ready()와 동일
	$('.poem-stanza').addClass('highlight');
});